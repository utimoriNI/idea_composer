<% provide(:title, 'ユーザー一覧') %>
<h1>ユーザー一覧</h1>


<%= paginate @users %>

<ul class="users">
  <% @users.each do |user| %>
  <%= render @users %>
  <% end %>
</ul>

<%= paginate @users %>
