パスワードの変更を行う場合は下のリンクをクリックしてください。

<%= link_to "パスワードの変更", edit_password_reset_url(@user.reset_token,
                                                        email: @user.email) %>

このリンクは送信から二時間後に無効になります
